//suppose we want to store the shopping list in the arrays
const shopping_list = ['bread', 'milk', 'butter', 'cheese', 'maggy', 'biscuit'];
console.log(shopping_list);

//finding the length of the array
var length_of_array = shopping_list.length;
console.log(length_of_array);

//acessing and modifying the arrays items
shopping_list[0] = "paneer";
console.log(shopping_list)

